export declare class SterlingType {
    private readonly _data;
    private readonly _children;
    constructor(data: any[], parent?: SterlingType);
    includes(atom: any, strict?: boolean): boolean;
}
